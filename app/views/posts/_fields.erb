<!-- @author_ids = Author.pluck(:id, :name) -->
<%= p.select(:author_id, { :options => @author } ) %>

<% field_set_tag do %>
  <%= p.label :title %>
<%= p.text_field :title %>
        <% end %>

<% field_set_tag do %>
  <%= p.label :body %>
  <%= p.text_area :body, :class => "form-control", :rows => "20" %>
        <% end %>

<%= submit_tag "Publish" %>
