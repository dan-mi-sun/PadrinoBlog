<h3>Index of Authors.</h3>
<table class="table">
  <tr><th>ID</th><th>First Name</th><th>Last Name</th><th></th><th>Email</th><th>Website</th><th></th><th>Created At</th><th>Updated At</th><th>Show</th><th>Edit</th><th>Delete</th></tr>
  <% @author_list.each do |author| %>
    <tr>
    <% author.attributes.each do |attribute, values| %>
      <td> <%= values %></td>
  <% end %>
    <td> <%= link_to 'Show', url_for(:authors, :show, :id => author.id)  %></td>
    <td> <%= link_to 'Edit', url_for(:authors, :edit, :id => author.id)  %></td>
    <td> <%= link_to 'Delete', url_for(:authors, :destroy, :id => author.id)  %></td>
  </tr>
<% end %>
</table>
